alert('Olá :)!!! Este programa calcula a área e o perímetro de um triângulo') // mostra mensagem para o usuário

const a = Number(prompt('Por favor, digite a dimensão do primeiro lado do triângulo'))
const b = Number(prompt('Por favor, digite a dimensão do segundo lado do triângulo'))
const c = Number(prompt('Por favor, digite a dimensão do terceiro lado do triângulo'))

let perimetro = (a) + (b) + (c)

alert('O perímetro deste triângulo é: '+ perimetro)

alert('Atualize a página para realizar novo cálculo')