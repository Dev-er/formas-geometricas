alert('Olá :)!!! Este programa calcula a área e o perímetro de um quadrado') // mostra mensagem para o usuário

const lado = prompt('Por favor, digite a dimensão do lado do quadrado')

let area = (prompt('A área deste quadrado é: ' + (lado * lado)))
let perimetro = Number(prompt('O perímetro deste quadrado é: ' + (4 * lado)))

alert('Atualize a página para realizar novo cálculo')