alert('Olá :)!!! Este programa calcula a área e o perímetro de um retângulo') // mostra mensagem para o usuário

const base = prompt('Por favor, digite a dimensão da base do retângulo')
const altura = prompt('Muito bem, agora informe a dimensão da alura do retângulo')

let area = (prompt('A área deste retângulo é: ' + (base * altura)))
let perimetro = Number(prompt('O perímetro deste retângulo é: ' + (2*(base * altura))))

alert('Atualize a página para realizar novo cálculo')